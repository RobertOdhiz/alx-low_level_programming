#include "main.h"
/**
* add - returns sum f two integers
* @a: first number
* @b: second number to be addded
* Return: sum of the two numbers
*/
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
