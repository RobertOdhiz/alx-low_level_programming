#include "main.h"

/**
 * print_triangle - prints a triangle
 * @size: triangle size
 */

void print_triangle(int size)
{
	int x, y;

	for (x = 0; x < size; x++)
	{
		for (y = i; y < size; y++)
			_putchar(' ');
		for (y = 1; y <= i; y++)
			_putchar('#');
		_putchar('\n');
	}
}
